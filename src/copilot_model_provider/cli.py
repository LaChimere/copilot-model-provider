"""CLI entrypoint for the copilot model provider package."""


def main() -> None:
    """Run the default command-line entrypoint for local smoke checks.

    The scaffold still exposes only a placeholder CLI, so this function
    intentionally performs a minimal action instead of starting the provider
    service or claiming runtime support that does not exist yet.
    """
    print('Hello from copilot-model-provider!')
